<%= link_to "Back to Pictures", pictures_url %> | <%= link_to "Edit", edit_picture_path(@picture) %> | <%= link_to delete_picture_path(@picture), method: :delete, data: {confirm: "Are you sure you want to delete this picture?"} %>

<p><%= @picture.title %>, <%= @picture.artist %></p>
<!-- <img src="<%= @picture.url %>"> -->
<%= image_tag @picture.url %>
